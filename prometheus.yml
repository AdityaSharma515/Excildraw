global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'http-backend'
    scheme: https
    metrics_path: /api/v1/metrics
    static_configs:
      - targets: ['http.sketchflow.adityashop.shop/']